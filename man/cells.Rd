% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_model.R, R/design_generics.R,
%   R/event-classes.R, R/event_vector.R
\name{cells.baseline_model}
\alias{cells.baseline_model}
\alias{cells}
\alias{cells.event}
\alias{cells.event_term}
\alias{cells.covariate_convolved_term}
\title{Extract cells from a design object}
\usage{
\method{cells}{baseline_model}(x, drop.empty = TRUE, ...)

cells(x, drop.empty = TRUE, ...)

\method{cells}{event}(x, drop.empty = TRUE, ...)

\method{cells}{event_term}(x, drop.empty = TRUE, ...)

\method{cells}{covariate_convolved_term}(x, ...)
}
\arguments{
\item{x}{The object to extract cells from.}

\item{drop.empty}{Logical indicating whether to drop empty cells (default: TRUE).}

\item{...}{Additional arguments (e.g., exclude_basis for convolved_term method).}
}
\value{
A data.frame/tibble of cells (categorical combinations) relevant to \code{x}.
}
\description{
Extract cells from a design object
}
\examples{
# Create sample event data with multiple factors
events <- data.frame(
  onset = c(2, 6, 10, 14, 18, 22),
  stimulus = factor(c("Face", "House", "Face", "House", "Face", "House")),
  emotion = factor(c("Happy", "Sad", "Happy", "Sad", "Neutral", "Neutral")),
  block = c(1, 1, 1, 2, 2, 2)
)

# Create sampling frame
sf <- fmrihrf::sampling_frame(blocklens = c(12, 12), TR = 2)

# Create event model with factorial design
model <- event_model(
  onset ~ hrf(stimulus, emotion),
  data = events,
  block = ~block,
  sampling_frame = sf
)

# Extract cells (factor level combinations) from event term
term1 <- terms(model)[[1]]
cells(term1)

# Create baseline model
bm <- baseline_model(
  ~ poly(block, 2) + Poly(1:24, 2),
  data = data.frame(block = c(rep(1, 12), rep(2, 12))),
  sampling_frame = sf
)

# Extract cells from baseline model
cells(bm)
}
