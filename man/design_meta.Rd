% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_metadata.R
\name{design_meta}
\alias{design_meta}
\title{Accessor: column metadata for a design matrix}
\usage{
design_meta(x)
}
\arguments{
\item{x}{A design matrix or any object with a \code{design_matrix()} method.}
}
\value{
A tibble, or \code{NULL}.
}
\description{
Returns the \code{col_metadata} tibble attached to a design matrix produced by
\code{event_model()}. If the attribute is missing (e.g., on a manually
constructed matrix) returns \code{NULL}.
}
\examples{
des <- data.frame(
  onset = c(0, 10, 20, 30),
  run = 1,
  cond = factor(c("A", "B", "A", "B"))
)
sframe <- fmrihrf::sampling_frame(blocklens = 40, TR = 1)
emod <- event_model(onset ~ hrf(cond), data = des,
                    block = ~run, sampling_frame = sframe)
design_meta(emod)
}
