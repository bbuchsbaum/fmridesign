% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate.R, R/feature.R, R/fmrihrf-reexports.R
\name{nbasis.covariate_convolved_term}
\alias{nbasis.covariate_convolved_term}
\alias{nbasis.feature_term}
\alias{fmrihrf-generics}
\title{Accessor generics shared with fmrihrf}
\usage{
\method{nbasis}{covariate_convolved_term}(x, ...)

\method{nbasis}{feature_term}(x, ...)
}
\arguments{
\item{x}{An object.}

\item{...}{Passed to methods.}
}
\value{
See \code{\link[fmrihrf:onsets]{fmrihrf::onsets()}}, \code{\link[fmrihrf:durations]{fmrihrf::durations()}},
\code{\link[fmrihrf:blockids]{fmrihrf::blockids()}} and \code{\link[fmrihrf:nbasis]{fmrihrf::nbasis()}}.
}
\description{
\code{onsets()}, \code{durations()}, \code{blockids()} and \code{nbasis()} are fmrihrf's
generics, re-exported by fmridesign. fmridesign registers methods on them
for its own classes, so \code{fmrihrf::onsets(term)} and \code{fmridesign::onsets(term)}
are the same function and dispatch to the same methods, whichever package
is attached first.

fmridesign provides methods for:
\itemize{
\item \code{onsets()}, \code{durations()}: \code{event_term}, \code{convolved_term}
\item \code{blockids()}: \code{event_term}, \code{convolved_term}, \code{event_model}
\item \code{nbasis()}: \code{hrfspec}, \code{convolved_term} subclasses, \code{feature_term}
and the parametric basis classes (\code{Poly}, \code{BSpline}, \code{Scale}, ...)
}
Methods for fmrihrf's own classes (\code{Reg}, \code{HRF}, \code{sampling_frame}) are
documented in fmrihrf.
}
\examples{
term <- event_term(
  list(condition = factor(c("A", "B", "A"))),
  onsets = c(0, 10, 20),
  blockids = c(1, 1, 1),
  durations = c(2, 2, 2)
)
onsets(term)
durations(term)
blockids(term)
identical(onsets, fmrihrf::onsets)
}
