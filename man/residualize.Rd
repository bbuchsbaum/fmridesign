% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_generics.R
\name{residualize}
\alias{residualize}
\title{Residualize Data Against a Design}
\usage{
residualize(x, data, cols = NULL, ...)
}
\arguments{
\item{x}{A design object (\code{event_model}, \code{baseline_model}) or a numeric matrix (design matrix X).}

\item{data}{A numeric vector/matrix/data.frame of observations Y with rows matching \code{nrow(design_matrix(x))}.}

\item{cols}{Optional integer or character vector selecting columns of the design to project out.}

\item{...}{Additional arguments passed to methods.}
}
\value{
Residuals with the same dimensions as \code{data}.
}
\description{
Projects the data onto the orthogonal complement of the design, returning
residuals from an OLS fit. Specialized for \code{event_model}, \code{baseline_model},
or a raw numeric design matrix.
}
\examples{
# Simple example with a raw design matrix
X <- cbind(1, 1:5)
Y <- cbind(1:5, 2:6)
R <- residualize(X, Y)
dim(R)
}
