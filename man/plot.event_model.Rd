% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_map.R
\name{plot.event_model}
\alias{plot.event_model}
\title{Plot Event Model}
\usage{
\method{plot}{event_model}(
  x,
  term_name = NULL,
  facet_threshold = Inf,
  label_mode = c("auto", "compact", "none"),
  max_labels = 30,
  abbrev_min = 10,
  strip_text_size = 8,
  ...
)
}
\arguments{
\item{x}{An \code{event_model} object.}

\item{term_name}{Character. Name of specific term to plot. If NULL, plots all terms.}

\item{facet_threshold}{Integer. Switch to faceting when number of regressors exceeds this value. Default 6.}

\item{label_mode}{Character. One of \code{"auto"}, \code{"compact"}, \code{"none"}. In \code{"auto"} mode
the method abbreviates labels for moderate counts and suppresses labels entirely
when they are excessive (> \code{max_labels}). \code{"compact"} always abbreviates labels.
\code{"none"} suppresses legend and facet strip labels.}

\item{max_labels}{Integer. When \code{label_mode = "auto"} and the number of regressors
exceeds this value, labels are suppressed. Default 30.}

\item{abbrev_min}{Integer. Minimum length used by \code{\link[base:abbreviate]{base::abbreviate()}} when compacting labels. Default 10.}

\item{strip_text_size}{Numeric. Strip label text size when faceting with labels. Default 8.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot2 object showing the predicted BOLD timecourses.
}
\description{
Creates a line plot visualization of the predicted BOLD response for each
regressor in an event_model object.
}
\details{
This method attempts to keep labels readable when there are many
regressors (e.g., trial-wise designs) by switching to faceting and either
abbreviating or suppressing labels depending on thresholds. You can control
this behavior via \code{label_mode}, \code{max_labels}, and \code{abbrev_min}.
}
\examples{
# Create a simple event model
des <- data.frame(
  onset = c(0, 10, 20, 30),
  run = 1,
  cond = factor(c("A", "B", "A", "B"))
)
sframe <- fmrihrf::sampling_frame(blocklens = 40, TR = 1)
emod <- event_model(onset ~ hrf(cond), data = des, block = ~run, sampling_frame = sframe)

# Plot all regressors
plot(emod)

# Plot specific term only
plot(emod, term_name = "cond")

}
