% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_event_model.R
\name{plot.event_model}
\alias{plot.event_model}
\title{Plot Event Model}
\usage{
\method{plot}{event_model}(
  x,
  term_name = NULL,
  style = c("auto", "stacked", "overlay", "heatmap"),
  show_events = TRUE,
  facet_threshold = Inf,
  label_mode = c("auto", "compact", "none"),
  max_labels = 30,
  abbrev_min = 10,
  strip_text_size = 8.5,
  block_x = c("global", "run"),
  facet_by_block = FALSE,
  show_block_bounds = TRUE,
  y_scale = c("term", "row", "shared"),
  time_range = NULL,
  title = NULL,
  subtitle = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{event_model} object.}

\item{term_name}{Character. Name of a specific term to plot. If \code{NULL},
plots all terms.}

\item{style}{Layout; one of \code{"auto"}, \code{"stacked"}, \code{"overlay"}, \code{"heatmap"}.}

\item{show_events}{Logical; draw event onset marks (rugs in stacked and
overlay styles, ticks in heatmap style). Default \code{TRUE}.}

\item{facet_threshold}{Integer. In \code{"overlay"} style, switch to one panel
per regressor when the number of regressors exceeds this value.}

\item{label_mode}{Character. One of \code{"auto"}, \code{"compact"}, \code{"none"}.
\code{"compact"} abbreviates condition labels, \code{"none"} suppresses them, and
\code{"auto"} suppresses them when there are more than \code{max_labels}.}

\item{max_labels}{Integer. Label limit used by \code{label_mode = "auto"}.}

\item{abbrev_min}{Integer. Minimum length used by \code{\link[base:abbreviate]{base::abbreviate()}} when
compacting labels.}

\item{strip_text_size}{Numeric. Size of row and panel labels.}

\item{block_x}{Time axis for multi-run designs. \code{"global"} (default) uses
concatenated time so each run occupies its own x-range; \code{"run"} uses
run-relative time that restarts each run (combine with
\code{facet_by_block = TRUE} to avoid overlaying runs).}

\item{facet_by_block}{Logical; if \code{TRUE}, draw one column of panels per run.}

\item{show_block_bounds}{Logical; draw rules at each run's start and end.
Drawn only when a \code{sampling_frame} is available.}

\item{y_scale}{How stacked rows share their amplitude axis. \code{"term"}
(default) gives rows of the same term (and basis function) one common
scale, so conditions can be compared directly while terms with different
units (e.g. a parametric modulator) keep their own; \code{"row"} scales every
row independently; \code{"shared"} uses one scale for all rows.}

\item{time_range}{Optional numeric length-2 vector (seconds, in the units of
the time axis) to zoom into, e.g. \code{c(0, 60)} to inspect basis shapes.}

\item{title, subtitle}{Optional plot title and subtitle. \code{NULL} uses an
informative default; \code{NA} removes it.}

\item{...}{Unused; accepted for compatibility.}
}
\value{
A ggplot2 object.
}
\description{
Draws the convolved regressors of an \code{event_model} against time, with the
events that generate them marked underneath.
}
\details{
The default \code{style = "auto"} picks the layout that stays readable for the
design at hand:
\itemize{
\item \code{"stacked"}: one row per condition on a shared time axis. Each row is
scaled on its own so response shapes stay visible, and a rug under each
trace marks the onsets of that condition's events. Basis sets (e.g.
SPMG3) are drawn in the same row, distinguished by line type.
\item \code{"heatmap"}: one raster row per regressor, used automatically for large
designs such as trialwise (beta-series) models, where overlapping traces
are unreadable. Rows are ordered by first onset.
\item \code{"overlay"}: all regressors on one shared amplitude axis, useful for
comparing magnitudes directly.
}

Runs are shown as alternating light bands labelled along the top edge, with
thin rules at each run's start and end so late starts and overruns are
easy to spot. Lines never connect across a run boundary.
}
\examples{
des <- data.frame(
  onset = c(0, 10, 20, 30),
  run = 1,
  cond = factor(c("A", "B", "A", "B"))
)
sframe <- fmrihrf::sampling_frame(blocklens = 40, TR = 1)
emod <- event_model(onset ~ hrf(cond), data = des, block = ~run, sampling_frame = sframe)

plot(emod)
plot(emod, style = "overlay")
plot(emod, term_name = "cond")

}
