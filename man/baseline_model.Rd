% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_model.R
\name{baseline_model}
\alias{baseline_model}
\title{Construct a Baseline Model}
\usage{
baseline_model(
  basis = c("constant", "poly", "bs", "ns"),
  degree = 1,
  sframe,
  intercept = c("runwise", "global", "none"),
  nuisance_list = NULL,
  nuisance_check = c("warn", "error", "drop", "none"),
  na_action = c("drop", "zero", "median")
)
}
\arguments{
\item{basis}{Character; type of basis function ("constant", "poly", "bs", or "ns").}

\item{degree}{Integer; degree of the spline/polynomial function.}

\item{sframe}{A sampling_frame object.}

\item{intercept}{Character; whether to include an intercept ("runwise", "global", or "none").
Ignored when \code{basis == "constant"} because the drift term already
provides the constant baseline.}

\item{nuisance_list}{Optional list of nuisance matrices or data frames (one per fMRI block).}

\item{nuisance_check}{Character; how to handle nuisance diagnostics. \code{"warn"}
warns on construction-time problems, \code{"error"} stops, \code{"drop"} removes
non-finite, zero-variance, and rank-aliased columns with a warning, and
\code{"none"} skips these checks.}

\item{na_action}{Character; how to handle \code{NA} values in \code{nuisance_list}
columns before the diagnostics run. \code{"drop"} (default) leaves \code{NA}s in
place so any column containing one is treated as non-finite and removed by
\code{nuisance_check}. \code{"zero"} replaces \code{NA} with \code{0} (matching the fMRIPrep
leading-row convention) and \code{"median"} replaces \code{NA} with the column
median; both repair an isolated leading \code{NA} (e.g. in DVARS or framewise
displacement) so the regressor is retained. \code{NaN} and \code{Inf} are never
repaired and remain subject to the non-finite drop. Repair is applied even
when \code{nuisance_check = "none"}, preventing \code{NA}s from leaking into the
design matrix.}
}
\value{
An object of class "baseline_model".
}
\description{
Builds a baseline model to account for noise and non-event-related variance.
This model may include a drift term, a block intercept term, and nuisance regressors.
}
\examples{
sframe <- fmrihrf::sampling_frame(blocklens = c(100, 100), TR = 2)
bmod <- baseline_model(basis = "bs", degree = 3, sframe = sframe)
bmod_global <- baseline_model(basis = "bs", degree = 3, sframe = sframe, intercept = "global")
bmod_nointercept <- baseline_model(basis = "bs", degree = 3, sframe = sframe, intercept = "none")
stopifnot(ncol(design_matrix(bmod)) == 8)
}
