% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_baseline.R, R/sampling_frame.R
\name{plot.sampling_frame}
\alias{plot.sampling_frame}
\alias{print.sampling_frame}
\title{Print and Plot for sampling_frame}
\usage{
\method{plot}{sampling_frame}(
  x,
  style = c("timeline", "grid", "lane"),
  show_ticks = FALSE,
  tick_every = 5,
  events = NULL,
  title = NULL,
  subtitle = NULL,
  ...
)

\method{print}{sampling_frame}(x, ...)
}
\arguments{
\item{x}{A \code{sampling_frame} object created by \code{fmrihrf::sampling_frame()}.}

\item{style}{Plot style for \code{plot()}. One of \code{"timeline"} (default),
\code{"grid"} or \code{"lane"}. \code{"timeline"} draws one bar per run on a shared
time axis; \code{"grid"} shows the scans of each run as cells on a
within-run scan axis; \code{"lane"} is a compact single row of adjacent run
segments, annotated above each segment.}

\item{show_ticks}{Logical; for \code{plot()}, whether to show per-TR tick marks
along each run (timeline style only). Default \code{FALSE}.}

\item{tick_every}{Integer; draw a tick every \code{tick_every} TRs when
\code{show_ticks = TRUE} (timeline), or alternate cell shading every
\code{tick_every} scans (grid; defaults to 10 for runs over 100 scans).
Default \code{5}.}

\item{events}{For \code{plot()}, optional events to overlay as a coverage
check: an \code{event_model}, or a data frame with an \code{onset} column (seconds,
relative to the start of each run) and a \code{run} (or \code{block}) column. Each
onset is drawn as a small tick under its run, so runs without events or
with an empty tail stand out; onsets that fall outside their run are
counted in the caption.}

\item{title, subtitle}{For \code{plot()}, optional title and subtitle; the
defaults describe the acquisition (runs, scans, TR, total duration).}

\item{...}{Unused.}
}
\value{
For \code{print()}, returns \code{x} invisibly. For \code{plot()}, a ggplot object.
}
\description{
Custom print and plot methods for \code{sampling_frame} objects (from fmrihrf).
The print method provides a concise summary focused on runs/blocks, TR and
total scans without mentioning lower-level evaluation precision. The plot
method visualizes runs over time.
}
\examples{
sf <- fmrihrf::sampling_frame(blocklens = c(60, 120), TR = 2)
print(sf)
plot(sf)

}
