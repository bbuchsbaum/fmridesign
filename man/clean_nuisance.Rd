% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_model.R
\name{clean_nuisance}
\alias{clean_nuisance}
\title{Clean nuisance regressors by dropping rank-useless columns}
\usage{
clean_nuisance(
  nuisance_list,
  sframe,
  basis = c("constant", "poly", "bs", "ns"),
  degree = 1,
  intercept = c("runwise", "global", "none"),
  tol = sqrt(.Machine$double.eps),
  duplicate_threshold = 1 - sqrt(.Machine$double.eps),
  na_action = c("drop", "zero", "median")
)
}
\arguments{
\item{nuisance_list}{A list of numeric matrices or data frames, one per block.}

\item{sframe}{A sampling frame.}

\item{basis, degree, intercept}{Baseline model settings used to construct the
comparison baseline terms.}

\item{tol}{Numeric tolerance passed to QR rank checks.}

\item{duplicate_threshold}{Absolute correlation threshold used to flag
duplicate or near-duplicate columns.}

\item{na_action}{Character; how to handle \code{NA} values in \code{nuisance_list}
columns before the diagnostics run. \code{"drop"} (default) leaves \code{NA}s in
place so any column containing one is treated as non-finite. \code{"zero"}
replaces \code{NA} with \code{0} (matching the fMRIPrep leading-row convention) and
\code{"median"} replaces \code{NA} with the column median; both repair an isolated
leading \code{NA} (e.g. in DVARS or framewise displacement) so the regressor is
retained. \code{NaN} and \code{Inf} are never repaired and remain non-finite.}
}
\value{
A list with \code{nuisance_list} and \code{report} elements. Pass
\code{result$nuisance_list} to \code{baseline_model()}.
}
\description{
Drops nuisance columns that are non-finite, zero-variance, or fail to
increase QR rank after the block's baseline terms and earlier nuisance
columns. Column order is respected, so when two columns are aliased the
earlier column is kept.
}
\examples{
sframe <- fmrihrf::sampling_frame(blocklens = 20, TR = 1)
motion <- cbind(
  linear = seq_len(20),
  duplicate = seq_len(20),
  constant = 1
)
cleaned <- clean_nuisance(list(motion), sframe, basis = "constant")
colnames(cleaned$nuisance_list[[1]])
}
