% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature.R
\name{feature}
\alias{feature}
\title{Continuous Feature Term}
\usage{
feature(
  x,
  dt = NULL,
  times = NULL,
  start = 0,
  center = TRUE,
  scale = c("none", "sd"),
  mask = NULL,
  basis = "spmg1",
  nbasis = 1,
  lag = 0,
  span = NULL,
  precision = NULL,
  id = NULL,
  name = NULL,
  prefix = NULL
)
}
\arguments{
\item{x}{Numeric vector, matrix (samples x features), or a list of those
(one per run).}

\item{dt}{Positive sampling interval in seconds. Mutually exclusive with
\code{times}.}

\item{times}{Sample times in seconds, or a list of per-run time vectors.
Mutually exclusive with \code{dt}.}

\item{start}{Start time in seconds used only when \code{dt} is supplied.
Defaults to 0.}

\item{center, scale, mask, span}{Passed to \code{\link[fmrihrf:feature_regressor]{fmrihrf::feature_regressor()}}.
Centering and scaling are applied per run.}

\item{basis, nbasis, lag}{HRF specification, as in \code{\link[=hrf]{hrf()}}.}

\item{precision}{Optional evaluation precision in seconds. When \code{NULL}
(default), the event-model precision is used, then tightened to
\code{min(precision, dt)}.}

\item{id, name, prefix}{Term identifier used for column names. If omitted,
the name of \code{x} is used.}
}
\value{
A \code{featurespec} object for use on the RHS of an \code{\link[=event_model]{event_model()}}
formula or in the list interface.
}
\description{
Specify a continuously sampled feature (for example RMS energy) to be
convolved with an HRF via \code{\link[fmrihrf:feature_regressor]{fmrihrf::feature_regressor()}}. Unlike \code{\link[=hrf]{hrf()}},
this is a time series, not a list of trials.
}
\details{
\code{x} may be a numeric vector or a samples-by-features matrix (one run), or a
list of those with one element per \code{sampling_frame} block. Supply exactly
one of \code{dt} or \code{times}. Feature-only models are allowed:
\code{event_model(~ feature(x, dt = 0.1, id = "rms"), sampling_frame = sframe)}.
}
\examples{
dt <- 0.5
rms <- abs(sin(seq(0, 20, by = dt)))
sframe <- sampling_frame(blocklens = 15, TR = 2)
emod <- event_model(
  ~ feature(rms, dt = dt, id = "rms", center = FALSE, scale = "none"),
  sampling_frame = sframe
)
ncol(design_matrix(emod))

}
\seealso{
\code{\link[fmrihrf:feature_regressor]{fmrihrf::feature_regressor()}}, \code{\link[=hrf]{hrf()}}, \code{\link[=event_model]{event_model()}}
}
