% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate.R, R/design_generics.R,
%   R/event_model.R, R/event_vector.R, R/feature.R
\name{longnames.covariate_term}
\alias{longnames.covariate_term}
\alias{longnames.covariate_convolved_term}
\alias{longnames}
\alias{longnames.event_model}
\alias{longnames.convolved_term}
\alias{longnames.event_seq}
\alias{longnames.feature_term}
\title{Extract longnames}
\usage{
\method{longnames}{covariate_term}(x, ...)

\method{longnames}{covariate_convolved_term}(x, ...)

longnames(x, ...)

\method{longnames}{event_model}(x, drop.empty = TRUE, expand_basis = FALSE, ...)

\method{longnames}{convolved_term}(x, ...)

\method{longnames}{event_seq}(x, ...)

\method{longnames}{feature_term}(x, ...)
}
\arguments{
\item{x}{The object.}

\item{...}{Additional arguments.}

\item{drop.empty}{Passed to \code{\link[=conditions]{conditions()}}.}

\item{expand_basis}{Logical; add a basis suffix for multi-basis HRFs
(default \code{FALSE}).}
}
\value{
Character vector of long (fully qualified) names.
}
\description{
Superseded by \code{conditions(x, style = "canonical")}.
}
\details{
Long names are the canonical condition names: each factor level is
written \code{variable.level}, and the factors of an interaction are joined
with \verb{_} (for example \code{condition.A_attn.x}). With \code{expand_basis = TRUE}
a \verb{_bNN} suffix is added for each HRF basis function.

They are not the design-matrix column names. A term's columns are
\code{paste0(term_tag, "_", longnames(term, expand_basis = TRUE))}, so the
column for level \code{A} of \code{hrf(condition)} is \code{condition_condition.A}, and
\code{longnames(term)} returns \code{condition.A}. Use \code{\link[=columns]{columns()}} (or
\code{colnames(design_matrix(x))}) for the column names, and \code{\link[=condition_map]{condition_map()}}
for a table that lines the two up. One exception: long names span the
full grid of factor levels, so an interaction cell with no events is
named here but has no design-matrix column.

Methods exist for \code{event_term}, \code{event_model} (all terms in order),
\code{convolved_term}, \code{feature_term}, covariate terms, and bare events from
\code{\link[=event_factor]{event_factor()}} and friends (\code{event_seq}), which are named as the
single-variable \code{event_term} built from them would be.
}
\examples{
# Create a simple event term with one condition factor
term <- event_term(
  list(condition = factor(c("A", "B", "A"))),
  onsets = c(0, 10, 20),
  blockids = c(1, 1, 1)
)
longnames(term)  # Returns: "condition.A" "condition.B"

# Create event term with multiple factors
term2 <- event_term(
  list(
    category = factor(c("face", "scene", "face")),
    attention = factor(c("attend", "attend", "ignore"))
  ),
  onsets = c(0, 10, 20),
  blockids = c(1, 1, 1)
)
longnames(term2)
# Returns: "category.face_attention.attend"
#          "category.scene_attention.attend"
#          "category.face_attention.ignore"

# Long names versus design-matrix column names
sf <- fmrihrf::sampling_frame(blocklens = 30, TR = 2)
des <- data.frame(onset = c(0, 10, 20), run = 1,
                  condition = factor(c("A", "B", "A")))
em <- event_model(onset ~ hrf(condition), data = des, block = ~run,
                  sampling_frame = sf)
longnames(em)  # "condition.A" "condition.B"
columns(em)    # "condition_condition.A" "condition_condition.B"
}
