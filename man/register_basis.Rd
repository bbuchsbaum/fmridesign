% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension_registry.R
\name{register_basis}
\alias{register_basis}
\title{Register a Parametric Basis Class}
\usage{
register_basis(
  class_name,
  prefix = NULL,
  modulation = c("parametric", "amplitude"),
  description = NULL
)
}
\arguments{
\item{class_name}{Character string naming the S3 class extending
\code{ParametricBasis}.}

\item{prefix}{Character string used as the term-tag prefix for terms whose
sole variable is \code{class_name(var, ...)}. Use \code{NULL} (or omit) if the
basis should not contribute a tag prefix (e.g., \code{Ident}).}

\item{modulation}{Character string describing the modulation kind for
columns generated by this basis. Defaults to \code{"parametric"}. Use
\code{"amplitude"} for an unmodulated identity-style basis.}

\item{description}{Optional human-readable description.}
}
\value{
Invisibly returns the registration entry.
}
\description{
Register a basis class so that the term-naming pipeline produces a
well-formed term tag (\code{<prefix>_<varname>}) and so that design-matrix
metadata identifies columns built from this basis as \code{parametric}.
Built-in bases (\code{Poly}, \code{BSpline}, \code{Scale}, \code{Standardized},
\code{ScaleWithin}, \code{RobustScale}, \code{Ident}) are registered automatically.
}
\examples{
\donttest{
# Register a custom orthogonal-polynomial basis with prefix "ortho"
register_basis("OrthoPoly", prefix = "ortho")
}
}
