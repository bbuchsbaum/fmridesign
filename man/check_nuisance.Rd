% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_model.R
\name{check_nuisance}
\alias{check_nuisance}
\title{Check nuisance regressors for rank and column problems}
\usage{
check_nuisance(
  nuisance_list,
  sframe,
  basis = c("constant", "poly", "bs", "ns"),
  degree = 1,
  intercept = c("runwise", "global", "none"),
  tol = sqrt(.Machine$double.eps),
  duplicate_threshold = 1 - sqrt(.Machine$double.eps)
)
}
\arguments{
\item{nuisance_list}{A list of numeric matrices or data frames, one per block.}

\item{sframe}{A sampling frame.}

\item{basis, degree, intercept}{Baseline model settings used to construct the
comparison baseline terms.}

\item{tol}{Numeric tolerance passed to QR rank checks.}

\item{duplicate_threshold}{Absolute correlation threshold used to flag
duplicate or near-duplicate columns.}
}
\value{
A \code{nuisance_check} object with \code{ok}, \code{problems}, \code{by_block}, and
normalized \code{nuisance_list} elements.
}
\description{
Inspects a block-wise nuisance regressor list before it is added to a
baseline model. The check is run per block and compares nuisance columns
against the baseline terms that would be constructed from \code{basis}, \code{degree},
and \code{intercept}.
}
