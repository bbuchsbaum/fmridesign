% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hrf-formula.R
\name{boxcar_hrf_gen}
\alias{boxcar_hrf_gen}
\title{Create duration-based boxcar HRF generator}
\usage{
boxcar_hrf_gen(normalize = TRUE, min_duration = 0.1)
}
\arguments{
\item{normalize}{Logical; whether to normalize the boxcar HRF. Default TRUE.}

\item{min_duration}{Numeric; minimum duration to use (prevents zero-width boxcars). Default 0.1.}
}
\value{
A function that takes an event data frame and returns a list of HRF objects.
}
\description{
Creates a generator function for use with the \code{hrf_fun} parameter in \code{hrf()}.
The generator produces boxcar HRFs where each event's duration determines
the boxcar width.
}
\examples{
# Events with variable durations
trial_data <- data.frame(
  onset = c(0, 10, 25),
  duration = c(2, 5, 3),
  condition = c("A", "B", "A"),
  run = 1
)
sf <- fmrihrf::sampling_frame(blocklens = 50, TR = 2)

emod <- event_model(
  onset ~ hrf(condition, hrf_fun = boxcar_hrf_gen()),
  data = trial_data, block = ~run, sampling_frame = sf, durations = trial_data$duration
)
print(emod)

}
\seealso{
\code{\link[=weighted_hrf_gen]{weighted_hrf_gen()}} for weighted impulse HRFs
}
