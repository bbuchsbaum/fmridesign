% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_generics.R, R/event_vector.R
\name{split_onsets}
\alias{split_onsets}
\alias{split_onsets.event_term}
\title{Split onsets}
\usage{
split_onsets(x, sframe, global = FALSE, blocksplit = FALSE, ...)

\method{split_onsets}{event_term}(x, sframe, global = FALSE, blocksplit = FALSE, ...)
}
\arguments{
\item{x}{The object.}

\item{sframe}{The sampling frame object containing timing information.}

\item{global}{Whether onsets are in global time units (across all runs).}

\item{blocksplit}{Whether to split onsets by blocks.}

\item{...}{Additional arguments.}
}
\value{
A list of onset vectors, one per block (unless \code{global=TRUE}).
}
\description{
Split onsets
}
\examples{
# Create sample event data across multiple blocks
events <- data.frame(
  onset = c(2, 6, 10, 24, 28, 32),
  condition = factor(c("A", "B", "A", "B", "A", "B")),
  block = c(1, 1, 1, 2, 2, 2)
)

# Create sampling frame with 2 blocks
sf <- fmrihrf::sampling_frame(blocklens = c(15, 15), TR = 2)

# Create event term
term <- event_term(
  list(condition = events$condition),
  onsets = events$onset,
  blockids = events$block
)

# Split onsets by block (default)
onsets_split <- split_onsets(term, sf, blocksplit = TRUE)
length(onsets_split)  # Number of blocks
onsets_split

# Global onsets (not split by block)
onsets_global <- split_onsets(term, sf, global = TRUE)
onsets_global
}
