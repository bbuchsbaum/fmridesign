% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrast_pipeline.R
\name{contrast_from_mask}
\alias{contrast_from_mask}
\title{Package a base mask into a \code{contrast} object}
\usage{
contrast_from_mask(mask, spec, term, classes = character())
}
\arguments{
\item{mask}{Output of \code{contrast_mask()} (or compatible list).}

\item{spec}{The originating contrast spec (stored on the result).}

\item{term}{The \code{event_term} the contrast is being computed against.}

\item{classes}{Character vector of extra S3 classes prepended to the
default \code{c("cell_contrast", "contrast", "list")}.}
}
\value{
A \code{contrast} object.
}
\description{
Drives the unified post-processing pipeline: expands the base mask across
basis functions (when applicable), applies basis filtering, attaches the
standard \code{contrast} list class, and returns an object compatible with
downstream \code{contrast_weights.event_model()}.
}
\details{
Custom contrast types implement \code{\link[=contrast_mask]{contrast_mask()}} and call this driver.
}
