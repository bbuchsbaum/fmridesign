% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmaps.R
\name{correlation_map.event_model}
\alias{correlation_map.event_model}
\alias{correlation_map.baseline_model}
\title{Regressor correlation heatmap}
\usage{
\method{correlation_map}{event_model}(
  x,
  rotate_x_text = TRUE,
  method = c("pearson", "spearman"),
  half_matrix = TRUE,
  limits = c("fixed", "data"),
  absolute_limits = NULL,
  annotate = NULL,
  flag_threshold = 0.5,
  vif_threshold = 5,
  title = "Regressor correlations",
  subtitle = NULL,
  within_run = FALSE,
  label_values = NULL,
  ...
)

\method{correlation_map}{baseline_model}(
  x,
  method = c("pearson", "spearman"),
  half_matrix = TRUE,
  limits = c("fixed", "data"),
  absolute_limits = NULL,
  rotate_x_text = TRUE,
  annotate = NULL,
  flag_threshold = 0.5,
  vif_threshold = 5,
  title = "Baseline regressor correlations",
  subtitle = NULL,
  within_run = TRUE,
  label_values = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{event_model} or \code{baseline_model}.}

\item{rotate_x_text}{Logical; angle column labels when they would overlap.}

\item{method}{Correlation method, \code{"pearson"} (default) or \code{"spearman"}.
VIFs are always based on linear regression.}

\item{half_matrix}{Logical; if \code{TRUE} (default) draw only the lower
triangle. With \code{FALSE}, both triangles are drawn.}

\item{limits}{\code{"fixed"} (default) spans the fill scale over [-1, 1];
\code{"data"} makes it symmetric about 0 up to the largest abs(r), and says
so in the legend title.}

\item{absolute_limits}{Deprecated logical kept for compatibility; \code{FALSE}
is equivalent to \code{limits = "data"}.}

\item{annotate}{Logical or \code{NULL}; print r in each cell. \code{NULL} (default)
annotates when there are at most 20 columns for an event model, or at
most 12 columns (after removing run intercepts) for a baseline model,
the threshold fmrireg's former baseline method used.}

\item{flag_threshold}{Cells with abs(r) at or above this value are
outlined.}

\item{vif_threshold}{VIFs at or above this value are flagged.}

\item{title, subtitle}{Plot title and subtitle.}

\item{within_run}{Logical. If \code{TRUE}, drop the run-intercept columns,
centre every column within each run, and correlate a column that is
non-zero in only one run (a run-specific drift or nuisance column) on
that run's scans only; pairs of columns specific to different runs share
no scans and are not drawn. This is the correlation structure the model
actually estimates with per-run intercepts. The default is \code{TRUE} for
baseline models, whose columns are mostly run-specific, and \code{FALSE} for
event models (runs concatenated).}

\item{label_values}{An alias for \code{annotate}, kept for compatibility with
fmrireg's former \code{correlation_map()} methods. Supply one or the other.}

\item{...}{Passed to \code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}}. Anything that is not a
\code{geom_tile()} argument or aesthetic is an error.}
}
\value{
A ggplot object.
}
\description{
Shows the pairwise correlations between design-matrix columns as the lower
triangle of a matrix. The fill is the package's diverging palette, fixed
at [-1, 1] by default with a neutral zero; for up to 20 columns each cell
also prints r (two decimals for up to 10 columns). Cells with abs(r) at or
above \code{flag_threshold} are outlined and counted in the subtitle, which
also names the most strongly correlated pair.
}
\details{
Pairwise correlations miss multicollinearity, so the diagonal shows each
column's variance inflation factor, VIF_j = 1 / (1 - R2_j), from
regressing column j on all other columns after removing each run's mean
(per-run intercepts). VIFs at or above \code{vif_threshold} are drawn in a
warning colour, and a column that is an exact linear combination of
others is marked "aliased".

Correlations are computed over all scans of the full design matrix, with
runs concatenated (as stated in the caption). Columns keep design order and
are grouped by term, so the map lines up with \code{\link[=design_map]{design_map()}}.
}
\examples{
des <- data.frame(
  onset = c(0, 10, 20, 30),
  run = 1,
  cond = factor(c("A", "B", "A", "B"))
)
sframe <- fmrihrf::sampling_frame(blocklens = 40, TR = 1)
emod <- event_model(onset ~ hrf(cond), data = des, block = ~run,
                    sampling_frame = sframe)
correlation_map(emod)
bmod <- baseline_model(basis = "poly", degree = 2,
                       sframe = fmrihrf::sampling_frame(c(40, 40), TR = 1))
correlation_map(bmod)
}
\seealso{
\code{\link[=design_map]{design_map()}}, \code{\link[=check_collinearity]{check_collinearity()}}
}
