% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_heatmaps.R
\name{design_map.event_model}
\alias{design_map.event_model}
\alias{design_map.baseline_model}
\title{Design matrix heatmap}
\usage{
\method{design_map}{event_model}(
  x,
  block_separators = TRUE,
  rotate_x_text = TRUE,
  fill_midpoint = NULL,
  fill_limits = NULL,
  scale = c("column", "none"),
  palette = c("diverging", "grey"),
  y_axis = c("scan", "time"),
  title = "Design matrix",
  subtitle = NULL,
  max_labels = 60,
  ...
)

\method{design_map}{baseline_model}(
  x,
  block_separators = TRUE,
  rotate_x_text = TRUE,
  fill_midpoint = NULL,
  fill_limits = NULL,
  scale = c("column", "none"),
  palette = c("diverging", "grey"),
  y_axis = c("scan", "time"),
  title = "Baseline design matrix",
  subtitle = NULL,
  max_labels = 60,
  ...
)
}
\arguments{
\item{x}{An \code{event_model} or \code{baseline_model}.}

\item{block_separators}{Logical; draw a rule at each run boundary.}

\item{rotate_x_text}{Logical; angle the column labels when they would
otherwise overlap. With \code{FALSE}, labels are always horizontal.}

\item{fill_midpoint}{Numeric or \code{NULL}. Centre of the diverging fill
scale; defaults to 0.}

\item{fill_limits}{Numeric length-2 vector or \code{NULL}. Fill-scale limits;
by default symmetric about \code{fill_midpoint} (diverging) or the data range
(grey).}

\item{scale}{\code{"column"} (default) divides each column by its maximum
absolute value; \code{"none"} plots raw values.}

\item{palette}{\code{"diverging"} (default; the package's signed palette,
symmetric about zero) or \code{"grey"} (white at the minimum to ink at the
maximum, as in SPM).}

\item{y_axis}{Label the right-hand axis by \code{"scan"} number (default) or
onset \code{"time"} (min:s, from the sampling frame).}

\item{title, subtitle}{Plot title and subtitle. The default subtitle states
the number of regressors, runs, and scans, and the TR, and names any
near-flat columns.}

\item{max_labels}{Maximum number of column labels to print; beyond this
labels are thinned to every k-th column.}

\item{...}{Passed to \code{\link[ggplot2:geom_tile]{ggplot2::geom_raster()}}.}
}
\value{
A ggplot object.
}
\description{
Draws the design matrix as an image in the style of SPM: one row per scan
(time runs downward), one column per regressor. Columns are grouped by
model term under a spanner, runs are separated by thin rules and labelled
in the left margin, and the right margin marks the first scan of each run.
}
\details{
By default each column is divided by its maximum absolute value
(\code{scale = "column"}, stated in the legend) so regressors in different units
share one fill scale; zero is always the neutral midpoint of the
package's diverging palette. Because rescaling would make a near-flat
regressor look healthy, real magnitudes stay visible: a strip of bars under
the matrix shows each column's peak absolute value relative to the largest
in its term, and columns below 5\% of that (or all zero) are marked in
orange, outlined over the matrix, and named in the subtitle. With 12 or
fewer columns each label also carries the column's raw range. Use
\code{scale = "none"} to plot raw values, or \code{palette = "grey"} for an SPM-style
greyscale.

For baseline models, columns are grouped by term (drift, intercept,
nuisance) and then run, with a "Run k" sub-spanner over each run's
columns, so the block-diagonal structure reads as a staircase; nuisance
columns are drawn at reduced opacity so they do not dominate.
}
\examples{
des <- data.frame(
  onset = c(0, 10, 20, 30, 5, 15, 25, 35),
  run = rep(1:2, each = 4),
  cond = factor(c("A", "B", "A", "B", "B", "A", "B", "A"))
)
sframe <- fmrihrf::sampling_frame(blocklens = c(40, 40), TR = 2)
emod <- event_model(onset ~ hrf(cond), data = des, block = ~run,
                    sampling_frame = sframe)
design_map(emod)
design_map(emod, palette = "grey", y_axis = "time")
bmod <- baseline_model(basis = "poly", degree = 3, sframe = sframe)
design_map(bmod)
}
\seealso{
\code{\link[=correlation_map]{correlation_map()}}, \code{\link[=plot_contrasts]{plot_contrasts()}}, \code{\link[=theme_fmridesign]{theme_fmridesign()}}
}
