% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_generics.R, R/event_vector.R
\name{regressors}
\alias{regressors}
\alias{regressors.event_term}
\title{Extract regressors}
\usage{
regressors(x, ...)

\method{regressors}{event_term}(x, hrf, sampling_frame, summate = FALSE, drop.empty = TRUE, ...)
}
\arguments{
\item{x}{The object.}

\item{...}{Additional arguments.}

\item{hrf}{HRF function}

\item{sampling_frame}{sampling_frame object}

\item{summate}{Logical; sum HRF responses}

\item{drop.empty}{Logical; drop empty conditions}
}
\value{
Character vector of regressor names for \code{x}.
}
\description{
Convolve the event-term design matrix with an HRF and return the
resulting regressors.
}
\examples{
# Create sample event data
events <- data.frame(
  onset = c(2, 6, 10, 14, 18),
  condition = factor(c("A", "B", "A", "B", "C")),
  rt = c(0.5, 0.8, 0.6, 0.9, 0.7),
  block = c(1, 1, 2, 2, 2)
)

# Create sampling frame
sf <- fmrihrf::sampling_frame(blocklens = c(10, 10), TR = 2)

# Create event term
term <- event_term(
  list(condition = events$condition, rt = events$rt),
  onsets = events$onset,
  blockids = events$block
)

# Get regressors with canonical HRF
hrf_canonical <- fmrihrf::HRF_SPMG1
regs <- regressors(term, hrf_canonical, sf)
head(regs)
dim(regs)

# With summation across conditions
regs_sum <- regressors(term, hrf_canonical, sf, summate = TRUE)
dim(regs_sum)
}
