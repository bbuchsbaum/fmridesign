% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrast_pipeline.R
\name{contrast_mask}
\alias{contrast_mask}
\title{Build a base contrast mask}
\usage{
contrast_mask(x, term, ...)
}
\arguments{
\item{x}{A \code{contrast_spec} object.}

\item{term}{An \code{event_term} against which the mask is computed.}

\item{...}{Additional arguments passed to methods.}
}
\value{
A list with elements:
\itemize{
\item \code{weights}   : numeric matrix, \code{n_base_conditions x n_contrast_cols},
row names = base condition names.
\item \code{condnames} : character vector of base condition names.
}
}
\description{
\code{contrast_mask()} is the extension point for new contrast types: it
returns a \emph{base} contrast matrix with one row per base condition (no
basis expansion) and one column per contrast column. The unified driver
\code{\link[=contrast_from_mask]{contrast_from_mask()}} handles basis expansion, basis filtering and
packaging into a \code{contrast} object compatible with
\code{contrast_weights.event_model()}.
}
\details{
To register a custom contrast type:
\enumerate{
\item Define a constructor that produces a list with class
\code{c("my_spec", "contrast_spec", "list")}.
\item Implement \code{contrast_mask.my_spec()} returning the base weights.
\item Implement \code{contrast_weights.my_spec()} as a one-liner:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{contrast_weights.my_spec <- function(x, term, ...) \{
  contrast_from_mask(contrast_mask(x, term, ...), x, term)
\}
}\if{html}{\out{</div>}}
}

The built-in spec classes (\code{pair_contrast_spec}, \code{oneway_contrast_spec},
\code{poly_contrast_spec}, ...) implement \code{contrast_weights()} directly, so
they do not require \code{contrast_mask()} methods.
}
